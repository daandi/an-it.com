!!! 5
%html
	=render 'layouts/head'
	%body
		%section{:id => 'page'}
			=render 'layouts/header'
			%section{:id => "main_section"}
				=yield
				%div{:class => "cleaner"}
				=render :partial=>'shared/like'
				
			=render 'layouts/footer'
